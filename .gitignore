__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
*.db
*.db-shm
*.db-wal
*.sqlite3
/data/
.pytest_cache/
.ruff_cache/
.idea/
.vscode/
*.log
dist/
build/
.playwright-mcp/
# Personal tool permissions; .claude/launch.json is shared deliberately.
.claude/settings.local.json
# Raw end-to-end capture artifacts (scripts/run_e2e_suite.py). Kept locally so
# runs can be diffed over time; the written analysis lives in docs/VALIDATION.md.
/validation_runs/
# Copyrighted course lecture slides, local reference only -- never publish these.
/class lectures/
